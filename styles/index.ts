import { createStitches } from '@stitches/react';

export const {
  config,
  styled,
  theme,
  css,
  createTheme,
  globalCss,
  keyframes,
  getCssText,
} = createStitches({

  theme: {
    colors: {
      defaultPurple: '#8257e6',
    }
  }
})